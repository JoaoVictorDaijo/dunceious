# IAR loop ledger — PR #78

`fix(viewer): codon_start-aware AA lane + prefer stored /translation (#70, #71)`

Focused IAR (single small commit): 3 fresh-context reviewers, distinct high-risk lenses, self-authored Workflow (premium-tier session), reviewers at Opus/xhigh.

| Round | Reviewers | Found | Fixed+verified | Surviving | Trend | Decision |
|------:|:----------|:------|:---------------|:----------|:------|:---------|
| 1 | 3 (lane sign · stored-translation alignment · broken-detection + render) | 2 Minor (0 Critical/Important) | 1 (doc-hardening of `isFeatureBroken`) | 0 Critical/Important | baseline | **converged** — 1 Minor left to owner |

## Round 1 detail

- **Lens A — lane sign (#70):** no findings. Confirmed the minus-strand `-phase` is correct and the issue's `+phase`-for-both sketch would have been wrong (exhaustive pairwise same-frame⇔same-lane sweep). Negative-modulo guard, codon_start=1 regression-equivalence, and parse-consistency with `extractCodingSequence` all verified.
- **Lens B — stored-translation alignment (#71):** no findings. `protein.length === codon-iteration count` for all lengths; alt-start Met lands on residue 0 (no double codon_start application); length mismatches and join/minus/trans-splice never shift the AA row.
- **Lens C — broken detection + render (#71):** 2 Minor, both edge/malformed-data only.
  1. *(high conf.)* A `/translation` that truncates before a real internal stop isn't flagged broken — **intended** per #71's contract (trust stored translation); standard GenBank stays covered (`*` caught, pseudogenes recomputed). **Action taken:** documented the limitation in `isFeatureBroken`'s JSDoc. No logic change (a recompute heuristic would revive the false positives the PR removes).
  2. *(low conf.)* broken=true with no glyph when stored `/translation` is *longer* than the coding sequence — unreachable with clean data (malformed annotation). Proposed won't-fix; owner call.

**Delta line:** Round 1: 2 found (0 Critical/Important); 1 fixed+verified this round (doc-hardening); 0 Critical/Important surviving; **converged**; 3 reviewer-runs so far.

Reviewer transcripts: `~/.claude/projects/-home-mainframe-dunceious/1b100b85-.../subagents/workflows/wf_bb0ae969-782/`

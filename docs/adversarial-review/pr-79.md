# IAR loop ledger — PR #79

Fix for #69: `parseLocation` no longer mis-flags scattered trans-spliced joins as
circular origin wraps (adds a `minStart === 0` conjunct to the wrap gate).

Mode: **lite** (single round). Reviewers: fresh-context, Opus/xhigh, self-authored
Workflow (session on a premium tier, so `pr-review-toolkit` was not invoked).

## Round 1

- **Reviewers:** 3 fresh-context lenses (predicate-correctness, regression/consumers,
  test-quality) + 6 adversarial verify agents.
- **Findings:** 6, **all CONFIRMED**, **all Minor** — 0 Critical, 0 Important.
- **Outcome:** **converged** (zero Critical/Important survive — the one automatic terminal).

| # | Finding | Sev | Chargeable | Disposition |
|---|---------|-----|-----------|-------------|
| 1 | Genuine origin wrap with base 1 in an intron → linear | Minor | yes (new) | won't-fix — already documented + pinned |
| 4 | Same class as #1 (`join(150000..154478,500..800)`) | Minor | yes (new) | won't-fix — duplicate of #1 |
| 2 | `minStart===0` necessary-not-sufficient: scattered join touching base 1 still false-wraps | Minor | no (pre-existing) | doc caveat added |
| 6 | Docstring overstated `minStart===0` as THE discriminator | Minor | no (doc) | doc caveat added |
| 5 | Mixed-strand (`usePerSegment`) envelope start/end never asserted | Minor | no (test gap) | envelope test added |
| 3 | `Row.tsx` connector loop not envelope-aware (rps12 dashes) | Minor | no (pre-existing) | tracked in issue #80 |

- **Fixes applied this round (behavior-neutral):**
  - Docstring: documented both residual directions (false-linear + false-wrap) of the
    base-1 heuristic — closes findings 2 & 6.
  - Test: `parseLocation` mixed-strand join now asserts its linear envelope — closes
    finding 5's blind spot on the `usePerSegment` path.
- **Verification:** full suite 599/0, `tsc` clean, license headers pass.
- **Delta:** 6 found; 0 Critical/Important; 3 Minor addressed (doc/test), 3 Minor
  won't-fix (documented / tracked in #80 / duplicate); **converged**; reviewer spawns: 3 + 6 verify.

## Owner decision

Converged on Minor-only findings. Awaiting owner sign-off on the remaining won't-fix
Minors (accepted limitation #1/#4, connector #3 → issue #80).

# IAR loop ledger — PR #83 (versioning reconstruction)

Mechanism: self-authored Workflow (3 fresh-context reviewers, Opus/xhigh). Lite (single round requested).
Handoff transcripts: `~/.claude/projects/-home-mainframe-dunceious/.../subagents/workflows/wf_3127b28e-f55/`

## Round 1 — 3 reviewers (ci-version-guard, version-scheme, docs-conventions)

10 raw findings → 6 distinct after dedup (three lenses independently flagged the guard's equality-vs-monotonic gap).

| # | Finding | Severity | Location | Action | Test |
|---|---|---|---|---|---|
| 1 | Current `2.4.2` is a **test-only bump** — by the PR's own table (`test → none`) the only change since v2.4.1 (render-test harness #68/#82) earns no bump; strict reading ⇒ current = `2.4.1` | Critical | package.json:4, CHANGELOG.md:15 vs CLAUDE.md bump table | needs owner decision | — |
| 2 | **version-guard forces a bump on EVERY promotion**, contradicting the table's `docs/test/chore/ci/build/refactor → none` rows — a docs/test-only promotion is hard-blocked | Important | ci.yml version-guard vs CLAUDE.md table | needs owner decision | — |
| 3 | **version-guard tests string-inequality, not a SemVer increase** — a downgrade/typo passes; and the corrective main `3.4.0 → 2.4.2` is itself a numeric downgrade | Important | ci.yml (Require-a-bump step) | needs owner decision | — |
| 4 | **main/develop diverged; main still `3.4.0`** — CLAUDE.md's present-tense "ships to users / what is live" is false now, and 5 tags (v2.1.0–v2.4.1) are **not reachable from main**, breaking the "tag = deployed" invariant (VERIFIED) | Important | CLAUDE.md:37,70-71; tags | needs owner decision | — |
| 5 | **CHANGELOG lists the held, untagged `2.4.2` as a dated release** — Keep-a-Changelog + the "2.4.2 held" policy ⇒ should be `Unreleased` | Important | CHANGELOG.md:15 | auto-fix (contingent on #1) | — |
| 6 | **Release-checklist step 1** (`git describe --tags ..develop`) breaks once tags go on main-merge commits (step 4) while main isn't an ancestor of develop | Minor | CLAUDE.md:58-59 | auto-fix (contingent on #4) | — |
| 7 | Reconstructed historical steps violate the going-forward bump table (frozen pre-policy history) | Minor | CHANGELOG.md (1.1.0/1.6.1/1.6.2/1.7.0) | proposed won't-fix (add caveat) | — |

**Root cause:** the reconstruction is a *library-style* "every wave = a version" history (numbers + tags on develop-line commits), but the going-forward policy is a *deploy-based* "version = what's live on main / tag = deployed" model. The PR ships both, and they don't line up (findings 1–4 are facets of this).

### Round 1 — owner decision & resolution

Owner chose **deploy-floor (keep 2.4.2)** + **fix first, then merge**. Fix package applied:

| # | Fix | Verified by |
|---|---|---|
| 1 | Deploy-floor added to the bump table (a promotion ships ≥ patch) — 2.4.2 is justified as the promotion floor | doc consistency w/ #2 |
| 2 | Same deploy-floor line; guard's every-promotion-bump is now the documented policy | — |
| 3 | Guard rewritten to require a **strict SemVer increase** (not mere inequality), with a self-removing one-time exception for the `3.4.0` corrective promotion | 8-case logic matrix (unchanged/patch/minor/major/downgrade/numeric/waived) + YAML validates; `setup-node` added |
| 4 | CLAUDE.md: "production still serves 3.4.0" note + first promotion flagged as a one-time downgrade; tag invariant re-anchored (v1.0.0–v2.4.1 = reconstructed develop milestones, not deployments) | git-verified reachability |
| 5 | CHANGELOG 2.4.2 → `## [Unreleased] — becomes 2.4.2 at next promotion` | — |
| 6 | Checklist step 1 uses `git tag --list --sort=-v:refname` (robust to main∤develop) instead of `git describe ..develop` | — |
| 7 | CHANGELOG note: historical bump *magnitudes* predate and aren't bound by the table | — |

**Delta line — Round 1: 6 distinct (1 Critical, 3 Important, 2 Minor); 6 fixed+verified (fix-diff re-review: guard logic unit-tested, headers pass 152/152, YAML valid); 0 surviving; baseline round; 3 reviewer-runs.**

**Status:** CONVERGED — zero Critical/Important surviving. Lite IAR (single round). Proceeding to merge into develop per the owner's decision.
